# Requirements
- npm +18.18
  - `brew install node`
  - I would suggest to use [`nvm`](https://github.com/nvm-sh/nvm) in order to have control over the versions in use 
- bower
  - `npm install -g bower` 
  