{
  "name": "eng-ladder-tool",
  "version": "1.0.0",
  "description": "- npm +18.18   - `brew install node`   - I would suggest to use [`nvm`](https://github.com/nvm-sh/nvm) in order to have control over the versions in use  - bower   - `npm install -g bower`",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server ./ -c-1 --cors",
    "precompile": "handlebars templates/ -f dist/templates.js -m",
    "watch:templates": "onchange 'templates/**/*.hbs' -- npm run precompile"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "onchange": "^7.1.0"
  }
}
